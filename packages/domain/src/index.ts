import type { DashboardSummary, JobRecord } from "@warsneaks/shared";
export const staleAfterHours = 24;
export function isStale(lastSyncedAt:string, now=new Date()){return now.getTime()-new Date(lastSyncedAt).getTime()>staleAfterHours*3600000}
export function transitionJob(job:JobRecord,status:JobRecord["status"],at=new Date().toISOString()):JobRecord{const allowed:Record<string,string[]>={queued:["running","cancelled"],running:["succeeded","partial","failed","cancelled"]};if(!allowed[job.status]?.includes(status))throw new Error(`Invalid job transition: ${job.status} -> ${status}`);return {...job,status,attempts:status==="running"?job.attempts+1:job.attempts,...(status==="running"?{startedAt:at}:{}),...(["succeeded","partial","failed","cancelled"].includes(status)?{finishedAt:at}:{})}}
export function demoSummary(jobs:JobRecord[]=[]):DashboardSummary{return {state:"stale",generatedAt:new Date().toISOString(),freshness:[{source:"Demo orders",lastSyncedAt:"2026-07-17T02:00:00Z",status:"stale"},{source:"Manual inventory",lastSyncedAt:"2026-07-19T01:00:00Z",status:"healthy"}],metrics:[{label:"Net sales",value:"Rp575.000",note:"Operational estimate"},{label:"Contribution profit",value:"Rp10.500",note:"Traceable to demo transactions"},{label:"Open actions",value:"3",note:"1 high priority"},{label:"Available stock",value:"5 unit",note:"WS-DEMO-01"}],jobs}}
