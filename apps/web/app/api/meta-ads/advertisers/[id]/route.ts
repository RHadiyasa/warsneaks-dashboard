import { NextResponse } from "next/server";import { z } from "zod";import { readSession } from "@web/lib/auth";import { updateAdvertiser } from "@web/lib/phase2/service";const schema=z.object({isWatched:z.boolean()});export async function PATCH(request:Request,{params}:{params:Promise<{id:string}>}){if(!await readSession())return NextResponse.json({code:"UNAUTHENTICATED"},{status:401});const parsed=schema.safeParse(await request.json());if(!parsed.success)return NextResponse.json({code:"VALIDATION_ERROR"},{status:400});try{return NextResponse.json(await updateAdvertiser((await params).id,parsed.data.isWatched))}catch{return NextResponse.json({code:"ADVERTISER_NOT_FOUND"},{status:404})}}