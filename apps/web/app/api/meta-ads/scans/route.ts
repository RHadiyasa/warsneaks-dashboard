import { NextResponse } from "next/server";import { z } from "zod";import { readSession } from "@web/lib/auth";import { getInbox,runScan } from "@web/lib/phase2/service";
const schema=z.object({keyword:z.string().trim().min(2).max(100),method:z.enum(["fixture","manual","playwright"]),payload:z.unknown().optional()});export async function GET(){if(!await readSession())return NextResponse.json({code:"UNAUTHENTICATED"},{status:401});return NextResponse.json(await getInbox())}export async function POST(request:Request){if(!await readSession())return NextResponse.json({code:"UNAUTHENTICATED"},{status:401});const parsed=schema.safeParse(await request.json());if(!parsed.success)return NextResponse.json({code:"VALIDATION_ERROR",issues:parsed.error.flatten()},{status:400});const scan=await runScan(parsed.data);return NextResponse.json(scan,{status:202})}