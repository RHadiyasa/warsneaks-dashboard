import { NextResponse } from "next/server";import { readSession } from "@web/lib/auth";import { findJob } from "@web/lib/store";export async function GET(_:Request,{params}:{params:Promise<{id:string}>}){if(!await readSession())return NextResponse.json({code:"UNAUTHENTICATED"},{status:401});const job=findJob((await params).id);return job?NextResponse.json(job):NextResponse.json({code:"JOB_NOT_FOUND"},{status:404})}
