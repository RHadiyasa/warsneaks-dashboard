export default function Login({searchParams}:{searchParams:Promise<{error?:string}>}){return <main className="login"><form method="post" action="/api/auth/login" className="login-card"><div className="brand-mark">W</div><p className="eyebrow">WAR SNEAKS</p><h1>Masuk ke command center</h1><p className="muted">Gunakan akun owner tunggal untuk mengakses data operasional.</p><label>Email<input name="email" type="email" defaultValue="owner@warsneaks.local" required/></label><label>Password<input name="password" type="password" defaultValue="change-me-before-production" required/></label><AsyncError value={searchParams}/><button type="submit">Masuk</button></form></main>}
async function AsyncError({value}:{value:Promise<{error?:string}>}){return (await value).error?<p className="error">Email atau password tidak valid.</p>:null}
