import fs from "node:fs/promises";import path from "node:path";import { parseManualImport,scanMetaAdsLibrary,type NormalizedMetaAd } from "../../../../packages/connectors/src/meta-ads";import { scoreOpportunity,type CanonicalAd } from "../../../../packages/domain/src/phase2";import { phase2Memory } from "./memory";
const workspaceId="demo-workspace";const dateOrNull=(value:string|null)=>{if(!value)return null;const date=new Date(value);return Number.isNaN(date.getTime())?null:date};
async function fixtureRows(){const text=await fs.readFile(path.join(process.cwd(),"fixtures/meta-ads/manual-import.sample.json"),"utf8");return parseManualImport(JSON.parse(text))}
async function persistScan(keyword:string,method:string,rows:NormalizedMetaAd[],failure?:{code:string;message:string}){if(!process.env.DATABASE_URL)return phase2Memory.run(keyword,rows,failure?.code);const {db}=await import("@warsneaks/db");return db.$transaction(async tx=>{const scan=await tx.adScan.create({data:{workspaceId,keyword,method,status:"running",startedAt:new Date()}});for(const row of rows){const advertiser=await tx.advertiser.upsert({where:{workspaceId_source_name:{workspaceId,source:row.source,name:row.advertiserName}},update:{pageUrl:row.advertiserUrl},create:{workspaceId,source:row.source,name:row.advertiserName,pageUrl:row.advertiserUrl}});const ad=await tx.competitorAd.upsert({where:{workspaceId_canonicalKey:{workspaceId,canonicalKey:row.canonicalKey}},update:{advertiserId:advertiser.id,body:row.body,headline:row.headline,cta:row.cta,landingPageUrl:row.landingPageUrl},create:{workspaceId,advertiserId:advertiser.id,source:row.source,sourceAdId:row.sourceAdId,canonicalKey:row.canonicalKey,contentFingerprint:row.contentFingerprint,body:row.body,headline:row.headline,cta:row.cta,landingPageUrl:row.landingPageUrl,startedRunningAt:dateOrNull(row.startedRunningAt)}});await tx.adObservation.create({data:{scanId:scan.id,competitorAdId:ad.id,observedAt:new Date(row.observation.observedAt),isActive:row.observation.isActive,platforms:row.observation.platforms,duplicateCount:row.observation.duplicateCount}})}return tx.adScan.update({where:{id:scan.id},data:{status:failure?(rows.length?"partial":"failed"):"succeeded",resultCount:rows.length,errorCode:failure?.code,errorMessage:failure?.message,finishedAt:new Date()}})})}
export async function runScan(input:{keyword:string;method:"fixture"|"manual"|"playwright";payload?:unknown}){let rows:NormalizedMetaAd[]=[];try{rows=input.method==="fixture"?await fixtureRows():input.method==="manual"?parseManualImport(input.payload): (await scanMetaAdsLibrary(input.keyword)).map(row=>parseManualImport({ads:[row]})[0]);return await persistScan(input.keyword,input.method,rows)}catch(error){const message=error instanceof Error?error.message:"SCAN_FAILED";return persistScan(input.keyword,input.method,rows,{code:message,message})}}
export async function getInbox(){if(!process.env.DATABASE_URL)return {ads:[...phase2Memory.ads.values()],scans:phase2Memory.scans,opportunities:phase2Memory.opportunities};const {db}=await import("@warsneaks/db");const [ads,scans,opportunities]=await Promise.all([db.competitorAd.findMany({where:{workspaceId},include:{advertiser:true,observations:{orderBy:{observedAt:"desc"}}},orderBy:{updatedAt:"desc"}}),db.adScan.findMany({where:{workspaceId},orderBy:{createdAt:"desc"},take:10}),db.opportunity.findMany({where:{workspaceId},include:{evidence:true,decisions:{orderBy:{createdAt:"desc"}}},orderBy:{createdAt:"desc"}})]);return {ads:ads.map(ad=>({...ad,startedRunningAt:ad.startedRunningAt?.toISOString()||null,createdAt:ad.createdAt.toISOString(),updatedAt:ad.updatedAt.toISOString(),observations:ad.observations.map(o=>({...o,observedAt:o.observedAt.toISOString(),createdAt:o.createdAt.toISOString()}))})),scans:scans.map(s=>({...s,createdAt:s.createdAt.toISOString(),startedAt:s.startedAt?.toISOString(),finishedAt:s.finishedAt?.toISOString()})),opportunities:opportunities.map(o=>({...o,createdAt:o.createdAt.toISOString(),updatedAt:o.updatedAt.toISOString(),decisions:o.decisions.map(d=>({...d,createdAt:d.createdAt.toISOString()}))}))}}
export async function updateAd(id:string,input:{isWatched?:boolean;tags?:string[];notes?:string}){if(!process.env.DATABASE_URL){const ad=phase2Memory.ads.get(id);if(!ad)throw new Error("AD_NOT_FOUND");Object.assign(ad,input);return ad}const {db}=await import("@warsneaks/db");return db.competitorAd.update({where:{id},data:input})}
export async function createOpportunity(input:{name:string;adIds:string[];reason:string;nextAction:string}){if(!process.env.DATABASE_URL)return phase2Memory.createOpportunity(input);const {db}=await import("@warsneaks/db");const ads=await db.competitorAd.findMany({where:{workspaceId,id:{in:input.adIds}},include:{observations:{orderBy:{observedAt:"asc"}}}});if(!ads.length)throw new Error("EVIDENCE_REQUIRED");const canonical=ads.map(ad=>({...ad,source:"meta_ads_library" as const,startedRunningAt:ad.startedRunningAt?.toISOString()||null,advertiserName:"",advertiserUrl:null,observation:{observedAt:"",isActive:null,platforms:[],duplicateCount:null},observations:ad.observations.map(o=>({observedAt:o.observedAt.toISOString(),isActive:o.isActive,platforms:o.platforms,duplicateCount:o.duplicateCount}))})) as unknown as CanonicalAd[];const scored=scoreOpportunity(canonical);return db.opportunity.create({data:{workspaceId,name:input.name,status:"watching",score:scored.score,confidence:scored.confidence,scoreVersion:scored.version,nextAction:input.nextAction,assumptions:`Score breakdown: ${JSON.stringify(scored.breakdown)}`,evidence:{create:ads.map(ad=>({competitorAdId:ad.id,reason:input.reason}))},decisions:{create:{toStatus:"watching",reason:"Opportunity created from Meta Ads evidence",actorId:"demo-owner"}}},include:{evidence:true,decisions:true}})}